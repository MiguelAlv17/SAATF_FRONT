# Base de la API para el build de producción (npm run build).
# Vite hornea esta variable en el bundle en tiempo de compilación.
# Los endpoints del código ya incluyen el prefijo /api, así que aquí va solo el origen.
VITE_API_BASE=https://api.saatf.qa.payfri-bi.com
